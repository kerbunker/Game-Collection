<div class="container">
  <a href="/dashboard"> &larr; Back to dashboard</a>
  <h2 class="mt-4">
    Edit List
  </h2>
  <form class="edit-list-form">
    <div>
      <input name="list-title" class="add-game-input"  type="text" value="{{list.title}}" />
    </div>
    <div class="m-2">
      {{format_date list.created_at}}
      |
      <a href="#game-list">{{list.games.length}}
        {{format_plural "game" list.games.length}}</a>
    </div>
    <button type="submit">Save list</button>
    <button type="button" class="delete-list-btn">Delete list</button>
  </form>
  <form class="game-form">
    <div class="mt-4">
      <h2>Add A New Game</h2>
    </div>
    <div class="mt-4">
      <input type="text" name="game-body" class="add-game-input"  placeholder="Game name"></input>
    </div>
    <div>
      <button type="submit">Add game</button>
    </div>
    {{> games list.games}}
    <a href="/dashboard/edit/{{game.id}}" class="edit-link">{{title}}</a>
    
  </form>
</article>

<script src="/javascript/edit-list.js"></script>
<script src="/javascript/delete-list.js"></script>
<script src="/javascript/game.js"></script>