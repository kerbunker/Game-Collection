<div class="games">
  {{#each this}}
  <section class="game">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{title}}
    </div>
  </section>
  {{/each}}
</div>